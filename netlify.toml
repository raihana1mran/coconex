# Netlify Configuration for Laravel
# ⚠️ Netlify doesn't support PHP natively

[build]
  publish = "public"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200